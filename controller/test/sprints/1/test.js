
//ping all services


//beforeall reset deployment in camunda and in neo4j

//upload model to config store (only bpmn)

// get config

//upload model to camunda

//check that model is in camunda

//delete model from camunda via the global delete endpoint

//check that model has been delete 

//


test('adds 1 + 2 to equal 3', () => {
    expect(true).toBe(3);
  });